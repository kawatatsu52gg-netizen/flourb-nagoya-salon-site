# Copy to `.env.local` for local development (do not commit secrets).
# Cloudflare Pages: set in Project → Settings → Environment variables (Production + Preview).

ANTHROPIC_API_KEY=

# Optional: absolute site URL for share links / metadata in client code if referenced later
NEXT_PUBLIC_SITE_URL=https://flourb.com
